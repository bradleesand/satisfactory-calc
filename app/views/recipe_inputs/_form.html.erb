<%= form_with(model: recipe_input, local: true) do |form| %>
  <% if recipe_input.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipe_input.errors.count, "error") %> prohibited this recipe_input from being saved:</h2>

      <ul>
      <% recipe_input.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :recipe_id %>
    <%= form.text_field :recipe_id %>
  </div>

  <div class="field">
    <%= form.label :resource_id %>
    <%= form.text_field :resource_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
